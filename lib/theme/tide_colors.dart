import 'package:flutter/material.dart';

class TideColors {
  static const primary = Color(0xFF5A8FB0);

  // Light Theme
  static const lightSurface = Color(0xFFFCFAF3);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightInk = Color(0xFF1A1A1A);
  static const lightMuted = Color(0xFF6B6B6B);

  // Dark Theme
  static const darkSurface = Color(0xFF121212);
  static const darkCard = Color(0xFF1E1E1E);
  static const darkInk = Colors.white;
  static const darkMuted = Color(0xFFB0B0B0);

  // Border
  static const border = Color(0xFFEFE9D8);

  // Priority Colors
  static const priHigh = Color(0xFFC76A55);
  static const priMed = Color(0xFFC9A23A);
  static const priLow = Color(0xFF7A9165);
}
